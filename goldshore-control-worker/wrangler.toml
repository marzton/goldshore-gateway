name = "goldshore-control-worker"
main = "src/index.ts"
compatibility_date = "2023-11-21"

[vars]
API_ENVIRONMENT = "production"
GOLD_SHORE_VERSION = "1.0.0"

# This worker should be private and only accessible via service bindings or Cloudflare Access.
# No routes are defined by default.

[[kv_namespaces]]
binding = "CONTROL_STORE"
id = "68f52b467dc0413991b2195ef9081cae" # GS_CONFIG namespace (shared config store)

[[services]]
binding = "api"
service = "gs-api"
environment = "prod"

[[services]]
binding = "gateway"
service = "gs-gateway"
environment = "prod"
